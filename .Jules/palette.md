## 2024-05-23 - Accessibility Improvements in Header
**Learning:** Adding `aria-label` and `aria-expanded` to mobile menu toggles and `aria-hidden` to decorative animations (like status pings) are high-impact, low-effort wins for accessibility.
**Action:** Always check icon-only buttons and decorative animations for ARIA attributes during component review.
